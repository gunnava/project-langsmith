[project]
name = "langsmith-support-agent"
version = "0.1.0"
description = "LangSmith support agent with RAG, LangGraph, and evaluation — LangChain Academy demo"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "langchain",
    "langgraph",
    "langsmith>=0.8.18",
    "langchain-community",
    "langchain-core",
    "langchain-openai",
    "langchain-text-splitters",
    "python-dotenv>=1.2.2",
    "scikit-learn",
    "pandas",
    "pyarrow",
    "lxml",
    "beautifulsoup4",
]

[tool.uv]
dev-dependencies = []
